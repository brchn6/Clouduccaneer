Metadata-Version: 2.4
Name: cloudbuccaneer
Version: 0.1.0
Summary: Fetch + fix SoundCloud downloads: yt-dlp wrapper + smart renamer
Requires-Python: >=3.9
Description-Content-Type: text/markdown
Requires-Dist: typer>=0.12
Requires-Dist: pyyaml>=6.0

# /home/barc/dev/CloudBuccaneer
# CloudBuccaneer

Fetch + fix SoundCloud downloads. Wraps `yt-dlp` for grabbing tracks/sets/likes, then cleans filenames with smart heuristics.

> Legal: Only download content you have the right to. This tool is for personal/offline use where permitted by law and by the site’s terms.

## Install
```bash
pip install -e .

